from setuptools import setup

setup(
    name="openrouter_selector",
    author="kent",
    description="LLM connection using OpenRouter with a single library.",
    version="0.1.0",
    packages=["openrouter_selector"],
    install_requires=["langchain"],
)

